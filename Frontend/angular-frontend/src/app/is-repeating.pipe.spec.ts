import { IsRepeatingPipe } from './is-repeating.pipe';

describe('IsRepeatingPipe', () => {
  it('create an instance', () => {
    const pipe = new IsRepeatingPipe();
    expect(pipe).toBeTruthy();
  });
});
