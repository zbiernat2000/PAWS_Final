import { GetTimePipePipe } from './get-time-pipe.pipe';

describe('GetTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
